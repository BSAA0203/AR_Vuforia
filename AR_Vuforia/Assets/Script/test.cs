using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class test : MonoBehaviour
{
    Vector3 xyz = new Vector3(15,15,15);
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //this.transform.Rotate(xyz*Time.deltaTime);
        //this.transform.Translate(xyz*Time.deltaTime);
        //this.transform.localScale+=xyz;
    }
}
